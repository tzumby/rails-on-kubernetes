apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: rails-kube-app
spec:
  ports:
    - port: 6379
  selector:
    app: rails-kube-app
    tier: cache
---
apiVersion: v1
kind: ReplicationController
metadata:
  name: redis
spec:
  replicas: 1
  selector: 
    app: rails-kube-app
    tier: cache
  template:
    metadata:
      name: redis
      labels:
        app: rails-kube-app
        tier: cache
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
